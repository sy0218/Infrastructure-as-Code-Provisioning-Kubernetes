# ===================================================
# [301-kafka-tools]
#   - Helm Chart 스택
#   - Kafka 운영 도구 3종
#     → schema-registry / kafka-ui / kafka-exporter
#
#   - 300-data-layer-base 가 만든 공용 오브젝트
#     (Namespace / ConfigMap / Secret)를 이름으로만 참조한다
#     → 300 설치 후에 배포할 것.
#   - ArgoCD 적용 시 Sync Wave 1 → 300 다음 배포
# ===================================================
apiVersion: v2
name: kafka-tools
description: "Kafka 운영 도구 → schema-registry(스키마 저장/검증) / kafka-ui(웹 콘솔) / kafka-exporter(Prometheus 메트릭)"
type: application
# 원본 규칙 승계: 버전은 정확 고정, 업그레이드는 숫자를 고치는 커밋으로만.
version: 0.1.0
