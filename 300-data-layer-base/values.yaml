# =====================================================
# [values.yaml]
# 300-data-layer-base 전용 설정
#
# 공통값(global.*)은 루트의 values.common.yaml에서 관리한다.
# 접속 주소 등 파생값은 templates/_helpers.tpl에서 global 값을 조합한다.
# =====================================================

# Neo4j 노드 라벨 접두사
# 운영: "" / 테스트: "Test"
graphLabelPrefix: "Test"