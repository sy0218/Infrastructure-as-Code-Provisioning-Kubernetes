# ===================================================
# [300-data-layer-base]
#   - Helm Chart 스택
#   - data-layer 공용 리소스 관리 주체
#     → Namespace / ConfigMap / Secret / ClusterRoleBinding
#
#   - 실제 애플리케이션(워크로드)는 없습니다.
#   - ArgoCD 적용 시 Sync Wave 0 → 가장 먼저 배포
# ===================================================
apiVersion: v2
name: data-layer-base
description: "data-layer 공용 오브젝트 → Namespace / ConfigMap data-layer-env / Secret data-layer-secrets / ClusterRoleBinding"
type: application
# 원본 규칙 승계: 버전은 정확 고정, 업그레이드는 숫자를 고치는 커밋으로만.
version: 0.1.0
