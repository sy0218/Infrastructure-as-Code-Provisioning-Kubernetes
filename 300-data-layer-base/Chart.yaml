# ===================================================
# [300-data-layer-base]
#   - Helm Chart 스택
#   - data-layer 워크로드 공용 리소스 관리 주체
#     → Namespace / ConfigMap / Secret / ClusterRoleBinding
# ===================================================
apiVersion: v2
name: data-layer-base
description: "data-layer 워크로드 공용 오브젝트 → Namespace / ConfigMap data-layer-env / Secret data-layer-secrets / ClusterRoleBinding"
type: application

# Chart 버전은 고정하며, 변경 시 명시적으로 버전을 올린다.
version: 0.1.0
