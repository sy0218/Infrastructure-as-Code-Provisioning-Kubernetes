# ===================================================
# [303-postgres]
# → 플랫폼 공용 PostgreSQL 배포
# → CNPG Cluster 1개 (Primary 1 + Replica 1)
# → Airflow / Collector / Iceberg 등의 DB 사용
#
# [배포 순서]
# 103-cnpg → 300-data-layer-base → 303-postgres → 304-...
# ===================================================
apiVersion: v2
name: postgres
description: "플랫폼 PostgreSQL → CNPG Cluster data-layer-postgres(2 인스턴스) / 앱 사용자 Secret / Database CR / 메트릭 - 외부 Service"
type: application

# Chart 버전 (업그레이드 시 명시적으로 변경)
version: 0.1.0