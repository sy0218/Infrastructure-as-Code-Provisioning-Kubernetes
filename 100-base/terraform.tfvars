# ===============================================
# [환경별 설정]
#   → variables.tf 의 default 대신 이 환경에서 쓸 값만 적는다.
# ===============================================

# 노드의 실제 디스크 마운트 경로 (default 없음 → 필수)
longhorn_data_path = "/data/longhorn"
