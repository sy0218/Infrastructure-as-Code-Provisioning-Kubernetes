# Terraform 작업 파일 (프로바이더 바이너리 캐시)
.terraform/

# state — 시크릿 값이 평문으로 남으므로 커밋 금지
*.tfstate
*.tfstate.*
*.tfstate.backup

# crash 로그
crash.log
crash.*.log

# 시크릿이 들어갈 수 있는 오버라이드 파일
*.auto.tfvars
override.tf
override.tf.json

# 클로드 코드 관련 파일
CLAUDE.md
COMMIT_CONVENTION.md