# ===============================================
# [환경별 설정]
#   → 변수 정의와 기본값 전체 목록은 variables.tf 참고.
# ===============================================

# ⚠ 이 값은 세 곳이 글자 그대로 같아야 한다 — 하나만 어긋나면 이름이 엉뚱한 곳을 가리킨다.
#   ① 여기(Service 가 요청하는 IP)
#   ② Ansible host.yml 의 ingress_vip (/etc/hosts 가 이름을 이 IP 로 푼다)
#   ③ 305-api 의 ingress_vip (파드 hostAliases — 파드도 같은 이름으로 나가야 한다)
ingress_vip = "192.168.56.240"
