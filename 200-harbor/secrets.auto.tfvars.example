# ===============================================
# [시크릿 주입 템플릿]
#   → 사용법: cp secrets.auto.tfvars.example secrets.auto.tfvars
#   → 차트의 harborAdminPassword 는 최초 설치에만 반영된다 — 이미 뜬 Harbor 는
#     UI/API 에서 먼저 바꾸고 여기를 같은 값으로 맞춘다.
#   → 아래는 랩 기본값 — 운영이면 반드시 교체할 것.
# ===============================================

# Harbor admin 비밀번호 — 차트 values 와 harbor 프로바이더 인증 공용
harbor_admin_password = "Harbor12345"
