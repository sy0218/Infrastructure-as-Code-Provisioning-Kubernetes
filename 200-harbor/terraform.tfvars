# ===============================================
# [환경별 설정]
#   → variables.tf 기본값으로 전부 동작한다 — 덮어쓸 값이 생길 때만 여기에 적는다.
#   → 비밀번호 등 시크릿은 여기가 아니라 secrets.auto.tfvars 로 넣는다(이 파일은 커밋된다).
# ===============================================
