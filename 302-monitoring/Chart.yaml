# ===================================================
# [302-monitoring]
#   - Helm Chart 스택
#   - 모니터링 3종 관리 주체
#     → Alloy(노드/컨테이너 메트릭 수집) / Prometheus(저장/질의) / Grafana(대시보드)
#
#   - 네임스페이스·공용 ConfigMap/Secret·API 권한은 300-data-layer-base 가 만든다.
#     → 이 차트는 300 설치 이후에 배포한다.
#   - ArgoCD 적용 시 Sync Wave 는 300 다음
# ===================================================
apiVersion: v2
name: monitoring
description: "모니터링 스택 → Alloy DaemonSet / Prometheus StatefulSet / Grafana Deployment + 각 Service·Ingress·PVC"
type: application
# 원본 규칙 승계: 버전은 정확 고정, 업그레이드는 숫자를 고치는 커밋으로만.
version: 0.1.0
