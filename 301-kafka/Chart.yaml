# ===================================================
# [301-kafka]
#
# Kafka(KRaft) 클러스터와 운영 도구를 배포한다.
#   - Kafka StatefulSet
#   - Schema Registry
#   - Kafka UI
#   - Kafka Exporter
#
# 설계
#   - hostNetwork + Local Disk 기반
#   - Broker는 특정 Node와 디스크에 고정
#   - Kubernetes는 다른 Node로 자동 재스케줄하지 않음
#
# 의존성
#   - 300-data-layer-base 선행 배포
#   - Ansible kafka_prereq로 /data/kafka-* 디렉터리 사전 준비
#   - ArgoCD Sync Wave: 1
# ===================================================
apiVersion: v2
name: kafka
description: "Kafka KRaft 클러스터 + Schema Registry, Kafka UI, Kafka Exporter"
type: application

# Chart 버전 (업그레이드 시 명시적으로 변경)
version: 0.1.0